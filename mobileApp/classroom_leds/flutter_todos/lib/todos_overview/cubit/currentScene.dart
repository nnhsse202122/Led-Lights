import 'package:flutter_todos/todos_overview/models/scene.dart';

void main() async {
  //Scene currentScene = await Scene.newScene();
}

// await (method that checks if a Scene starts at the current time)
